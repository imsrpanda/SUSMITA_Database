name: SQL Deployment

on:
  push:
    branches:
      - main

jobs:

  Deploy:

    runs-on: windows-latest

    steps:

    - name: Checkout
      uses: actions/checkout@v4

    - name: Display Files
      run: dir Database /s